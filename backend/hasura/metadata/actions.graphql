type Mutation {
  SignUp(
    email: String!
    password: String!
    beeer_name: String!
  ): SignUpOutput
}

type Mutation {
  Verify(
    id: uuid!
    code: String!
  ): verifyOutput
}

type SignUpOutput {
  id: uuid!
  token: String!
}

type verifyOutput {
  email: String!
  id: uuid!
  is_verified: Boolean!
}

